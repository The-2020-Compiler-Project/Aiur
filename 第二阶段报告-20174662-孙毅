词法分析器是编译器一个重要的组成部分，词法分析器的主要功能是识别单词和翻译单词。
识别单词是从用户的源程序中把单词分离出来；翻译单词是把单词转换成机内表示，便于后续处理。
词法分析器部分将从文件中读入的字符序列转换为单词（Token）序列，
在以文件形式读入的程序中，识别出具有各自独特意义的单词，即关键字、标识符、常量、界符等几大类，
并依次输出各单词的内部编码及单词符号本身值等属性。
我的小组的词法分析部分首先将程序中的空格符、换行符、注释等一些无用元素滤去，
进而对单词进行识别，主要分为关键字、标识符、算数常量、字符常量、界符（双目和单目）几个部分
词法分析器的功能是输入源程序段，输出Token序列，在输出的Token序列中包括两个元素：单词的类型，单词在所属表的位置。

目前各个模块基本完成，正在测试
